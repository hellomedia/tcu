controllers:
    # all controller files inside Controller/ and its subdirectories
    resource: '../src/Controller/**/*Controller.php' # must be a glob string for exclude to work
    type: attribute

security_controllers:
    resource: '../pack/Security/Controller/'
    type: attribute

status_controllers:
    resource: '../pack/Status/Controller/'
    type: attribute
