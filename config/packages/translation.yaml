framework:
    default_locale: '%default_locale%'
    enabled_locales: '%locales%'
    translator:
        default_path: '%kernel.project_dir%/translations'
        # additional paths
        paths:
            - '%kernel.project_dir%/pack/Security/translations'
        fallbacks: ['%default_locale%']
